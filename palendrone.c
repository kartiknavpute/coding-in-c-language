#include<stdio.h>
void main()
{
	int n,a,b,d,sum=0,s;
	printf("enter a number=");
	scanf("%d",&a);
	n=a;
	for(a!=0)
	{
		b=a%10;
		d=sum*10+b;
		s=a*10;
	}
	if(n=a)
	printf("the number is palendrone");
	else
	printf("the number is not palendrone");
}

